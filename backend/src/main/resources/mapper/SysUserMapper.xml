<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartauto.oa.system.mapper.SysUserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.smartauto.oa.system.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} AND del_flag = 0
    </select>

    <!-- 查询用户的权限标识列表 -->
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON rm.menu_id = m.id
        INNER JOIN sys_user_role ur ON ur.role_id = rm.role_id
        WHERE ur.user_id = #{userId}
          AND m.status = 1
          AND m.del_flag = 0
          AND m.perms IS NOT NULL
          AND m.perms != ''
    </select>

    <!-- 查询用户的角色Key列表 -->
    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        SELECT r.role_key
        FROM sys_role r
        INNER JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
          AND r.del_flag = 0
    </select>

    <!-- 插入用户角色关联 -->
    <insert id="insertUserRole">
        INSERT INTO sys_user_role (user_id, role_id) VALUES (#{userId}, #{roleId})
    </insert>

    <!-- 删除用户角色关联 -->
    <delete id="deleteUserRoleByUserId">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>

    <!-- 插入用户岗位关联 -->
    <insert id="insertUserPost">
        INSERT INTO sys_user_post (user_id, post_id) VALUES (#{userId}, #{postId})
    </insert>

    <!-- 删除用户岗位关联 -->
    <delete id="deleteUserPostByUserId">
        DELETE FROM sys_user_post WHERE user_id = #{userId}
    </delete>

</mapper>
